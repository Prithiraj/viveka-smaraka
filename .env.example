# Public canonical URL used for metadata, sitemap, and structured data.
NEXT_PUBLIC_SITE_URL=https://vivekasmaraka.org

# Optional institutional intake endpoint. The public interest form is hidden unless this is configured.
# INTEREST_WEBHOOK_URL=https://institution.example/intake

# Optional bearer token sent only from the server to the intake endpoint.
# INTEREST_WEBHOOK_TOKEN=replace-with-a-secret-token
